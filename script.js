// ========================== // ABRIR CONVITE // ========================== function abrirConvite() { const envelopeTela = document.getElementById("tela-envelope"); const conviteTela = document.getElementById("tela-convite"); const envelope = document.querySelector(".envelope"); envelope.classList.add("abrindo"); setTimeout(() => { envelopeTela.style.opacity = "0"; envelopeTela.style.visibility = "hidden"; conviteTela.style.opacity = "1"; conviteTela.style.visibility = "visible"; }, 1500); } // ========================== // VERIFICAR SE DEVE ABRIR // DIRETO NO CONVITE // ========================== document.addEventListener("DOMContentLoaded", () => { const parametros = new URLSearchParams(window.location.search); if (parametros.get("aberto") === "true") { const envelopeTela = document.getElementById("tela-envelope"); const conviteTela = document.getElementById("tela-convite"); if (envelopeTela && conviteTela) { envelopeTela.style.opacity = "0"; envelopeTela.style.visibility = "hidden"; conviteTela.style.opacity = "1"; conviteTela.style.visibility = "visible"; } } }); // ========================== // 🎁 LISTA DE PRESENTES // ========================== function listaPresentes() { window.location.href = "lista-presentes.html"; } // ========================== // 📍 LOCAL DA FESTA // ========================== function verLocal() { window.location.href = "https://maps.app.goo.gl/Tm5UPhYXr8d7YddF8?g_st=ac"; } // ========================== // 💌 CONFIRMAR PRESENÇA // ========================== function confirmarPresenca() { window.location.href = "https://yasmimfelix705-a11y.github.io/confirma-o-presen-a-15/"; } 
